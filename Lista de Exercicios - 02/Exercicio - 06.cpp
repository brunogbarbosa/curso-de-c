#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
      
      int impar, par, valor;
      
      printf("Digite um numero:" );
            scanf("%d", &valor);
      
      if (valor % 2 == 0) {
            printf("O Numero é par!");          
      } else {
            printf("O Numero é impar!");
      }
      
}
